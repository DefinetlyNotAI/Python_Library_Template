Use the 2 python files attached and modify for your needs,

Opne cmd inside the directory of the library

python setup.py sdist bdist_wheel
twine upload dist/* --verbose

Use the API key attached.